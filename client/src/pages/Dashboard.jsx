import {useQuery} from '@tanstack/react-query';import {Chart as ChartJS,ArcElement,Tooltip,Legend,CategoryScale,LinearScale,BarElement} from 'chart.js';import {Doughnut,Bar} from 'react-chartjs-2';import {AcademicCapIcon,BuildingOffice2Icon,CreditCardIcon,ExclamationTriangleIcon} from '@heroicons/react/24/outline';import api from '../services/api';import StatCard from '../components/StatCard';ChartJS.register(ArcElement,Tooltip,Legend,CategoryScale,LinearScale,BarElement);export default function Dashboard(){const {data,isLoading}=useQuery({queryKey:['dashboard'],queryFn:()=>api.get('/dashboard').then(r=>r.data.data)});const s=data?.stats||{};const chart={labels:['Occupied','Available'],datasets:[{data:[data?.occupancy||0,100-(data?.occupancy||0)],backgroundColor:['#635bff','#e9eaf4'],borderWidth:0}]};return <div><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Command center</p><h1>Good day, manage with confidence.</h1><p className="mt-1 text-slate-500">Live overview of your hostel ecosystem.</p></div><button className="btn">+ Add student</button></div>{isLoading?<div className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4">{[1,2,3,4].map(x=><div key={x} className="h-40 animate-pulse rounded-2xl bg-slate-200 dark:bg-slate-800"/>)}</div>:<><div className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Active students" value={s.student} detail="Current academic year" icon={AcademicCapIcon}/><StatCard label="Rooms managed" value={s.room} detail="Across all hostels" icon={BuildingOffice2Icon}/><StatCard label="Payments recorded" value={s.payment} detail="Financial ledger" icon={CreditCardIcon}/><StatCard label="Open complaints" value={s.complaint} detail="Needs attention" icon={ExclamationTriangleIcon}/></div><div className="mt-6 grid gap-6 xl:grid-cols-3"><article className="card xl:col-span-2"><div className="flex items-center justify-between"><div><h2>Operations snapshot</h2><p>Monthly movement across core functions</p></div><span className="pill">This month</span></div><div className="mt-8 h-64"><Bar data={{labels:['Students','Invoices','Payments','Visitors','Leaves','Complaints'],datasets:[{label:'Records',data:[s.student,s.invoice,s.payment,s.visitor,s.leave,s.complaint],backgroundColor:'#635bff',borderRadius:8}]}} options={{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{y:{beginAtZero:true,grid:{color:'#eef0f6'}},x:{grid:{display:false}}}}}/></div></article><article className="card"><h2>Live occupancy</h2><p>Bed allocation across hostels</p><div className="mx-auto mt-5 h-44"><Doughnut data={chart} options={{cutout:'75%',plugins:{legend:{display:false}}}}/></div><p className="text-center text-3xl font-bold">{data?.occupancy||0}%</p><p className="text-center text-sm text-slate-500">occupied beds</p></article></div><article className="card mt-6"><div className="flex justify-between"><div><h2>Recent activity</h2><p>Auditable actions from your team</p></div><span className="pill">Live</span></div><div className="mt-5 divide-y divide-slate-100 dark:divide-slate-800">{data?.recent?.length?data.recent.map(a=><div className="flex items-center justify-between py-3 text-sm" key={a._id}><span><b className="capitalize">{a.actor?.name||'System'}</b> {a.action}d {a.resource}</span><time className="text-slate-400">{new Date(a.createdAt).toLocaleString()}</time></div>):<p className="py-7 text-center text-sm text-slate-500">Activity will appear here as your team works.</p>}</div></article></>}</div>}
