import {useForm} from 'react-hook-form';import {Link} from 'react-router-dom';import toast from 'react-hot-toast';import api from '../services/api';export default function ForgotPassword(){const {register,handleSubmit}=useForm();return <main className="auth"><div className="auth-card"><h1>Reset your password</h1><p className="mb-7">We’ll send recovery instructions if your account exists.</p><form onSubmit={handleSubmit(async d=>{await api.post('/auth/forgot-password',d);toast.success('Check your inbox for recovery instructions')})}><label>Email<input type="email" {...register('email',{required:true})}/></label><button className="btn mt-5 w-full">Send instructions</button></form><Link className="mt-5 block text-sm text-brand" to="/login">Back to sign in</Link></div></main>}
