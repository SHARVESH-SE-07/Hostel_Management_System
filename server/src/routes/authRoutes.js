import {Router} from 'express'; import * as a from '../controllers/authController.js'; import {protect} from '../middlewares/auth.js'; const r=Router();r.post('/register',a.register);r.post('/login',a.login);r.post('/refresh',a.refresh);r.post('/logout',a.logout);r.post('/forgot-password',a.forgotPassword);r.post('/reset-password',a.resetPassword);r.get('/me',protect,a.me);export default r;
