import {models} from '../models/domain.js'; import AuditLog from '../models/AuditLog.js';
export const dashboard=async(req,res)=>{const keys=['Student','Room','Bed','Invoice','Payment','Complaint','Visitor','Leave'];const counts=await Promise.all(keys.map(k=>models[k].countDocuments()));const stats=Object.fromEntries(keys.map((k,i)=>[k.toLowerCase(),counts[i]]));const recent=await AuditLog.find().sort('-createdAt').limit(8).populate('actor','name role').lean();res.json({success:true,data:{stats,recent,occupancy:stats.bed?Math.round((await models.Bed.countDocuments({status:'occupied'}))/stats.bed*100):0}})};
